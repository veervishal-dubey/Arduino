void setup()
{
  Serial.begin(9600);
  pinMode(A0,INPUT);
  pinMode(13,OUTPUT);
}

void loop()
{
  int ldr=analogRead(A0);
  if (ldr<=400)
  	digitalWrite(13,HIGH);
  else
    digitalWrite(13,LOW);
  delay(1000);
}
